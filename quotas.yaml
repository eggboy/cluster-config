apiVersion: v1
kind: ResourceQuota
metadata:
  name: mem-cpu-dev
  namespace: dev
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    persistentvolumeclaims: "5"
    requests.storage: "10Gi"
